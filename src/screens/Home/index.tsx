import React from 'react';
import Container from '../../components/commons/Container';
import { Text } from 'react-native';

const Home = () => {
  return (
    <Container style={{ flex: 1 }} edges={['top', 'left', 'right']}>
      <Text>Dummy</Text>
    </Container>
  );
};

export default Home;
